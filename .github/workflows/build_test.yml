name: Makefile CI

on:
  push:
    branches: [ "master", "test" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Install prerequisites
      run: sudo apt-get update && sudo apt-get -y install mpi-default-dev flex libscotch-dev

    - uses: actions/checkout@v3

    - name: Update submodules
      run: git submodule update --init  --depth 1 --recursive

    - name: Build all
      run: make all -j2

    - name: Run tests
      run: make test

    - name: Archive logs
      if: success() || failure()
      uses: actions/upload-artifact@v4
      with:
        name: application logs
        path: |
          test_runs/**/log.*
          test_runs/**/*.log
